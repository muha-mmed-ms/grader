version: '3.2'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile_dev
    container_name: CS_LOCF_UI_DEV
    ports:
      - '8080:8080'
    network_mode: 'bridge'
    restart: unless-stopped
    env_file:
      - ./environments/.env.development
    volumes:
      - .:/app
      - /app/node_modules